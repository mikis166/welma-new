<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <div class="well">  
      <%= render "#{Plan.find(params[:plan]).name.downcase}_form" %>
    
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>